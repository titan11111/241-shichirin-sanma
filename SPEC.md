# 241-shichirin-sanma ｜ 七輪サンマ焼きバトル

## 0. ドキュメント情報

- 仕様書名: 七輪サンマ焼きバトル
- 対象ゲーム: `241-shichirin-sanma`（旧 `241-day065`）
- 作成日: 2026-09-16
- 更新日: 2026-09-16
- ステータス: 実装済み（harness PASS。iPhone 実機は未検証）
- 参照ファイル: `index.html`（単一ファイル）

## 批評（コントロールパネル実装前）

- 題材が良い。「両面を70〜90に収める」という一点にルールが絞られていて、説明が3行で済む。
- **60秒の勝負なのに止められなかった**。屋台の対戦という短距離の設計だからこそ、中断できないと1回ごとに賭けになる。
- じゅうじゅう音が鳴りっぱなしで消せない。iPhoneで開く前提としては出しにくい。
- うちわの長押しに `setPointerCapture` が無く、指が少しずれると火力が切れる（押しっぱなしバグの典型）。
- スコアが毎回消える。自己ベストが残らないと2回目の動機が弱い。
- 起動時の幕が canvas を覆い、DOM先頭の操作対象が押せない canvas になっていた。

## 1. ゲーム概要

- ジャンル: 3D 焼き加減対戦
- 一言説明: 屋台の焼き場のゲンと、60秒でサンマの焼き加減を competing する。
- 想定プレイ時間: 60秒／1本
- 想定プレイヤー: iPhone 片手（電車）
- クリア体験の要点: 両面とも「ベスト」で焼き上げて皿へ出す瞬間

## 2. 対象環境

### 必須
- 配信先: GitHub Pages
- 最優先端末: iPhone Safari
- 描画: Three.js r128（WebGL）
- 音: WebAudio でノイズ生成（音声ファイルなし。公開実体 0.03MB）

### 任意
- PC: Space=うちわ長押し / F=ひっくり返す / Enter=焼き上がり / P・Esc=ポーズ

## 3. ファイル構成

```text
241-shichirin-sanma/
  index.html
  SPEC.md
  LEARNINGS.md
```

## 4. 操作／設定UI（コントロールパネル）

| 項目 | 実装 |
|---|---|
| 入力方式（キー） | Space=うちわ / F=ひっくり返す / Enter=焼き上がり / **P・Esc=ポーズ** |
| 入力方式（タッチ） | 画面下の3ボタン（うちわ＝長押し／ひっくり返す／焼き上がり） |
| Pointer Events | うちわボタンで `setPointerCapture`。指がずれても長押しが切れない |
| ポーズ | 画面上部中央 `Ⅱ` ボタン、または P / Esc。`<dialog>` で「再開／音の切替／タイトルへ」 |
| 自動停止 | `visibilitychange` でタブを離れたら自動ポーズ |
| ミュート | 画面上部中央 `♪` ボタン（ポーズ画面からも切替可）。じゅうじゅう音のゲインを0にする |
| 設定の保存 | `tg.241.muted`（ミュート） / `tg.241.best`（自己ベストスコア） |
| 重なり順 | z-index で明示（canvas:0 / HUD・パッド:3 / 幕:5）。DOM順に依存しない |

- ポーズ中は `dt=0` にして**残り時間・焼き加減・煙・炭の明滅まで全部止まる**
- ポーズ中は操作3ボタンを `disabled` にする

## 5. ゲームルール

- 制限時間 60秒。下を向いている面だけが焼ける（裏面は 1/10 の速度）
- うちわ長押しで火力ブースト（最大2.2倍、離すと減衰）
- 「ひっくり返す」で焼き面を入れ替え（0.45秒のクールダウン）
- 「焼き上がり！」で皿へ出して確定。出さずに時間切れになると -10点

### 採点
| 焼き具合 | 判定 |
|---|---|
| 〜55 | 生 |
| 〜70 | ちょい生 |
| **70〜90** | **ベスト**（80がピークで100点） |
| 〜102 | 焼き強め |
| 102〜 | 焦げ |

- 総合点 = A面とB面の平均。相手（ゲン）と比較して勝敗を決める

### 対戦相手「焼き場のゲン」
- 目標値を 74〜90 の範囲でランダムに設定し、そこへ寄せてくる
- 20%の確率で「よそ見」して返すのが遅れる／残り時間が足りないとうちわを振り出す

## 6. 未確定事項

- iPhone 実機での発熱とフレームレート（未検証）
- three.js を CDN 参照している（235・237 と同じ方式）。同梱化は未着手
