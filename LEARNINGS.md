# 241-shichirin-sanma LEARNINGS

## 2026-09-19 団扇パタパタ音

- 紙のバンドパスノイズ＋竹の低い三角波。既存の WebAudio ノイズバッファを再利用。
- 1打ち1パタ。連打間隔が短いほど `flapBusy` が上がり、重ね打ち（最大3）と高い紙音になる。
- 長押しは熱に応じた周期で鳴る。キーリピートは無視（Spaceの自動連打で壊れない）。
- ミュート中は鳴らさない。実機の聞こえ方は未検証。

## 2026-09-19 影絵エンディング（三皿・三人）

- スコアボードをやめ、三皿を並べて客が食べる。判定は台詞。ステータスは増やしていない。
- 客は影絵のオリジナル三人。女優（高止まり）／常連の爺／わんぱく小僧。小僧は「カツオのよう」という指示だが、名前も顔も複製していない。
- 魚の色は焼いた dA/dB を皿に載せる。影は黒、食べ物だけ色がある。
- 台詞は textContent。皿のSVGは createElementNS。
- 検証: Playwright で `#ending` を開き、女優の一口目と、タップ飛ばし後の三人＋フィナーレを撮影。通しの三皿プレイと実機は未検証。

## 2026-09-19 三皿総合・日替わり火加減（ステータスは増やさない）

- 依頼は「3皿・総合判定・その日の火力と気温・団扇の微調整」。続けて「ステータスは複雑は嫌」。
- やらなかったこと: 火力ゲージ、気温メーター、焼き加減の数字、皿ごとの詳細スコア。見えるのはバーと「一皿」と秒だけ。本日条件はタイトルの一行。
- 日替わりは `YYYYMMDD` をハッシュして火力3段階＋気温16〜34℃。同じ日は同じ。HUDには出さない。
- 団扇は「長押しで加速」をやめた。熱を上げ、離すと当日の火へ戻る。強火の日は押しっぱなしで焦げる。弱火の日は起こさないと間に合わない。
- 自己ベストは1皿100点から三皿総合300点へ。旧値が残っていても次のプレイで上書きされる。
- 検証: 当日シードの再現と、強火放置／弱火放置の焼ける速さは node で確認。タイトル画面は Playwright。通しプレイと iPhone 実機は未検証。

## 2026-09-19 タイトルを昭和看板へ

- 題字を「七輪サンマ焼きバトル」から屋号「七輪」＋本題「秋刀魚」に短縮。「バトル」は昭和の屋台看板に乗らない。
- フォントは Chokokutai（彫刻体）。Dela Gothic のままでは現代の titling に見える。タイマー等の数字は Dela Gothic のまま残した。
- 朱の短冊＋金の横線。題字色は `var(--ink)` にして、ダークは生成り・ライトは墨、どちらでも消えないようにした。
- 検証: Playwright で iPhone 幅 390×844、dark / light を撮影。題字は両方で読める。iPhone 実機は未検証。

## 2026-09-16 コントロールパネル実装＋タイトル付与

- フォルダ名を `241-day065` から `241-shichirin-sanma` へ、エントリを `shichirin-sanma-battle.html` から `index.html` へ変更。
- **描画コストが致命的だった**。`antialias:true` ＋ `pixelRatio 2` で、ソフトウェア描画環境では 13 RAF/秒。`antialias:false` ＋ `pixelRatio 1.5` に変えるだけで 52 RAF/秒まで回復した（4倍）。235・237 が `antialias:false` を選んでいるのはこの理由。
- `shadowMap.enabled = true` だったが、**影を落とすライトが1つも無かった**。メッシュ側に `castShadow=true` を並べても、光源が `castShadow` でなければ影は1枚も出ない。無効化しても見た目は変わらない。
- **`setPointerCapture` が無く、うちわの長押しが指のズレで切れていた**。`pointerdown` で捕まえるだけで解決。共通方針が「61本しか入っていない・押しっぱなしバグの根因」と書いているとおりだった。
- ポーズは `dt=0` 方式にした。`now` を進めないので、残り時間だけでなく煙・炭の明滅・うちわの往復まで同時に止まる。状態を1箇所（時間の進み）で制御できる設計が効いた。
- 幕付きタイトル＋canvas の構成は harness のタップ判定で必ず FAIL する（台帳に2件記録あり）。今回は**幕を canvas より前へ移し、重なり順を z-index で明示**して解決した。DOM順に頼った重なりをやめれば、幕を前に置いても表示は崩れない。**偽陽性として見送らず実際に直せた3件目**。
- 検証: harness PASS ／ 操作盤の機能テスト **18/18 PASS**（ポーズで残り時間が止まる、Pキー・Escape、タブ非表示で自動ポーズ、リロード後もミュート保持、JSエラー0件）。
- 未検証: iPhone 実機の発熱・FPS。

## 2026-09-19 旧エントリURLの404を修復
- 症状: `https://titan11111.github.io/241-shichirin-sanma/shichirin-sanma-battle.html` が **404**。本体（`/241-shichirin-sanma/`）は 200 で生きていた
- 原因: エントリを `shichirin-sanma-battle.html` → `index.html` へ改名したため、**改名前に配ったリンクだけが死んだ**
- 対処: `shichirin-sanma-battle.html` を index.html へのリダイレクト専用ページとして復活（meta refresh ＋ `location.replace()`。`?query`・`#hash` も引き継ぐ）
- 検出元: `_tools/check-legacy-entry.sh`（245の同種事故を機に新設）。本番URLへcurlを撃って検出
- 鉄則8: エントリ名を変えたら旧名をリダイレクトで必ず残す（本体URLが200のままなので気づけない）

### 【訂正】上の「404だった」は誤り（2026-09-19 同日中に判明）
- gitで裏を取った結果、`shichirin-sanma-battle.html` は**このリポジトリで一度も公開されていなかった**（`git cat-file -e <修復コミット>^:shichirin-sanma-battle.html` → 不在）。
  改名はローカルフォルダ内で完結しており、リポジトリは改名**後**に作成されている
- つまり `…/241-shichirin-sanma/shichirin-sanma-battle.html` というURLは**元から存在しない**。「配ったリンクが死んだ」という上の記述は**誤り**
- 置いたリダイレクトは**害はないが、壊れていたものを直したわけではない**（将来その名前で来た人を受けるだけの保険）
- 誤認の原因: LEARNINGS.md の本文を証拠として扱ったこと。**本文は作業メモであって証拠ではない。証拠はgit履歴**
- 検出器も v2 で「git履歴に存在 かつ HEADに不在」判定へ作り直した（`_tools/check-legacy-entry.sh`）
## 2026-09-19 突風イベント

- 一皿ごとに開始9〜18秒後、その後も9〜18秒間隔で3〜5秒の風が吹くようにした。
- 風の間はプレイヤーとゲンの焼き進行を1.38倍にし、中央に一時表示を出す。公平性のため両者へ同じ倍率を適用する。
- タイトル画面の「焼き始める」から開始する既存導線は維持した。

## 2026-09-19 影絵の造形更新

- 記号的だった人物SVGを、横顔・冠／髪飾り・細い関節肢・金色の関節鋲・透かし彫りを持つアジア伝統影絵風のオリジナル造形へ更新した。
- 女優・常連・小僧の姿勢と装飾を分け、黒一色でも人物の違いが読めるようにした。
- 昭和らしさを補うため、鼻の誇張を抑えて人間の横顔へ寄せ、古い障子紙の光・木枠・フィルム粒子・手描き看板「七輪座 秋刀魚品評会」を舞台へ追加した。

## 2026-09-19 皿上の秋刀魚をプレイ結果へ連動

- エンディングの三皿は、各皿でプレイヤーが確定した `dA` / `dB` をグラデーションへ変換し、実際の焼き色で描画する。
- 皿を出した時点の `flipped` も保存し、最後に上を向いていた面を皿上の主面として描く。焼き筋も平均値ではなく、その上面の焼き値から決める。
- 食後表現で魚を `opacity: .22` まで薄くすると焼き色が判別できなかった。色は90%残し、頭側をギザギザに欠けさせる食べ跡へ変更した。
- 楕円表現をやめ、頭・胴・尾・目・口を備えた秋刀魚形状へ変更。焼きが進むほど焼き筋が増え、焦げ域では筋も濃くなる。

## 2026-09-19 昭和レトロ判定カット

- 影絵の三人が三皿を評価したあと、放射状の赤金背景と映画看板風の枠で「判定は！！！ ○○の勝利！」を2.6秒表示する。
- タップ／キー入力で送れる。引き分け時は事実に合わせて「両者引き分け！」と表示する。
## 2026-09-19 焼き場のハプニング5種

- 猫／カラス／脂の炎／灰かぶり／炭の弱りを追加。既存の団扇・返す操作と、一時的な「こらっ！」ボタンで対抗する。
- 猫だけは `getUserMedia` とWeb Audioの波形振幅を使い、端末内の声量で追い払える。録音・送信はせず、拒否時もボタン操作を残す。
- 失敗は即終了にせず皿の評価減点に留め、ランダム性が理不尽になりすぎないようにした。
